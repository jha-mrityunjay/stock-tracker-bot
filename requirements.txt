# No runtime dependencies — handler.py uses only the Python standard library,
# so the Lambda deployment package is just handler.py + nse_equity.json (~80 KB).
#
# This is deliberate: it keeps cold starts fast and means there is no build step
# (no pip install --target, no native wheels) that can break the deploy.
