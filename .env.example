# Copy to .env and fill in. Never commit .env.

# From @BotFather on Telegram
TELEGRAM_TOKEN=123456:ABC-DEF...

# Supabase project settings -> API
SUPABASE_URL=https://xxxxxxxx.supabase.co
SUPABASE_KEY=eyJhbGciOi...

# Upstox -> Developer Apps -> Analytics tab -> Generate Token
# Read-only, valid 1 year, no daily re-login. NOT the OAuth access token.
UPSTOX_TOKEN=eyJ0eXAiOi...

# Any random string you invent. Telegram sends it back on every webhook call so
# the Lambda can reject requests that aren't from Telegram.
# Generate one: python -c "import secrets; print(secrets.token_urlsafe(32))"
WEBHOOK_SECRET=
